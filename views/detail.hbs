<div class="container">
    <div class="row">
        <div class="col-6">
            <img src={{product.image}} class="card-img-top" alt="...">
        </div>
        <div class="col-6">
            <h1>{{product.title}}</h1>
            <div >
              <p>{{product.description}}</p>
            </div>
            <div class="d-flex justify-content-between">
                <a href="#" class="btn btn-primary">Sotib Olish</a>
                <button class="btn btn-warning" style="cursor: default;">Prise: {{product.price}}$</button>
            </div>
        </div>
    </div>
</div>